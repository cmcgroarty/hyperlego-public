import { ExplicitValidatorDirective } from './explicit-validator.directive';

describe('ExplicitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplicitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
