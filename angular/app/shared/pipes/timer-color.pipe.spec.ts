import { TimerColorPipe } from './timer-color.pipe';

describe('TimerColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerColorPipe();
    expect(pipe).toBeTruthy();
  });
});
